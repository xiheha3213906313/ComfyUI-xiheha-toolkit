# Changelog

## 0.9.1 - 2026-09-22

- 修复循环小数帧时刻被取整到毫秒后造成的首尾串帧：切片现在直接使用整数帧区间、精确帧率和输出帧数约束，显示时间不再参与 FFmpeg 寻址。
- 修复独立 AAC 切片在视频达到帧数上限时提前终止音频编码、导致末帧缺少部分采样的问题；视频与音频现在分别在滤镜层精确收尾，旧切片缓存会自动失效。
- 修复智能视频分割器刷新工作流后，分段模式和最短、目标、最长时长被创建默认值覆盖的问题。

## 0.9.0 - 2026-09-21

- 将智能视频分割器的六个底层算法选项精简为「智能自适应检测」「快速内容检测」「高运动抑制检测」三种场景模式；旧算法值不再兼容，历史工作流需要重新选择检测模式。
- 重构场景指标：使用颜色/亮度/边缘分布和去除 DC 分量的感知哈希组合检测硬切，并以滚动中位数、MAD 和局部显著度控制候选。
- 新增按需 Farneback 光流运动补偿；光流只用于抑制可由连贯平移、缩放或运镜解释的候选，不直接产生切镜分数。
- 新增淡入淡出、黑场/白场与保守叠化窗口检测，同一物理转场只保留一个候选；精确分段、输出结构和时长约束保持不变。
- 修复目标时长兜底导致真实切镜落入下一段最短时长禁区的问题；目标分段现在会一步前瞻并前移准备边界，同时继续保持最短/最长时长约束，旧规划缓存会自动重新计算。
- 调整目标分段选点：候选统一按目标时长距离排序，强切仅在距离相同时优先；扫描窗口增加局部上下文，避免边缘假峰导致误切。
- 将原“模糊”模式更名为“目标”，内部值改为 `target`；新增内部值为 `fuzzy` 的新“模糊”模式，达到最短时长后采用第一个可靠切镜点，无候选则在最长时长兜底，两种场景模式均保留一步前瞻。
- 分段模式内部值统一为 `target` / `fuzzy` / `exact`，默认改为 `target`；旧工作流不做兼容迁移，原 `fuzzy` 值会表示新的模糊模式，需要重新选择“目标”。
- 扩大渐变检测的窗口上下文，避免从过渡中段开始扫描时产生边缘伪叠化。
- 更新检测模式和高级参数说明；默认智能模式在六个标注视频的只读验收中以 ±1.25 秒容差达到 31/31 命中、2 个额外候选，平缓视频零误报。

## 0.8.4 - 2026-09-21

- 重新设计智能视频分割器的分段时长拖拽条：采用紧凑矩形模式按钮、原生输入深色轨道、直角不透明蓝色选区和全高竖条手柄，扩大拖拽命中区域并恢复单行时长读数。
- 优化模糊/精确模式切换与键盘微调；保留原有三点约束、工作流状态字段与 3.0~15.0 秒范围。
- 修复拖拽手柄越出节点后松开可能无法结束拖动的问题；现在会在指针释放、取消、捕获丢失、窗口失焦或页面隐藏时统一清理拖拽状态。
- 调整时间轴命中分区：蓝色区默认控制目标手柄，两侧黑色区分别控制相邻白色手柄，三个手柄的直接命中宽度统一为 12px。
- 最短与最长两个白色手柄始终保留至少 0.2 秒间距，状态恢复与拖拽过程使用同一约束。
- 将轨道填充与刻度放入独立圆角裁剪层，修复精确模式左侧蓝色选区越出槽体圆角的问题，同时保持手柄位于槽体上层。

## 0.8.3 - 2026-09-21

- 将提示词配置与视频接口从 `server.py` 拆到独立路由模块，路由入口保持幂等，五个 HTTP 路径、方法和请求字段不变。
- 新增共享视频 pipeline，统一节点执行与前端主动计算的参数校验、尺寸计算、分段、切片和 manifest 复用；节点执行继续复用缓存，前端计算继续主动重算。
- 视频接口增加容器、枚举、数值范围、节点 ID、绝对路径及目录穿越校验；元数据、进度与切分响应不再返回内部绝对路径或完整执行流。
- 将提示词配置编辑器和智能视频分割器拆为状态、视图、接口/时间轴与 controller 模块；保存使用不可变请求快照，节点移除时统一清理轮询、文件输入、拖动监听器和 widget callback。
- 将前端共享能力按 graph、widgets、layout、workflow、payload、prompt-flow、lifecycle 拆分；工作流变更优先使用 ComfyUI `captureCanvasState()`，移除合成鼠标事件。
- Tooltip 拆为纯解析与 DOM runtime，默认继续使用 ComfyUI 原生 Tooltip，定制卡片按需加载且只保留一处互斥逻辑。
- 删除运行时 CSS 字符串，`web/toolkit.css` 改为 base、prompt、config-editor、smart-video-splitter 样式清单；Tooltip 样式保持按需加载。
- 测试改为临时缓存目录并增加真实 ESM 模块、共享 pipeline、缓存复用、路由边界与路径脱敏回归。

## 0.8.2 - 2026-09-21

- 修复「智能视频分割器」上传或选择视频后，未手动保存工作流刷新网页视频丢失并回退为默认视频的问题。
- 视频选择与上传状态同步写入 `splitter_state` 并在 `onConfigure` 恢复时优先无损还原，自动补齐 widget 选项。
- 强化草稿持久化触发机制：上传与切换后自动触发 `afterChange`、`litegraph:canvas` 与草稿状态抓取，确保浏览器刷新保留最新选定视频。
- 优化视频预览地址生成，支持子目录（subfolder）视频资源的正确预览与元数据获取。
- 后端新增 `VALIDATE_INPUTS` 动态校验，确保新上传视频不被静态输入列表限制。

## 0.8.1 - 2026-09-20

- 修复「智能视频分割器」上传或切换视频后自动缩小节点的问题；自动布局现在会保留用户手动扩大的尺寸。
- 将该节点的最小尺寸调整为 350×700。

## 0.8.0 - 2026-09-20

- 新增「智能视频分割器」节点（`XH_SmartVideoSplitter`）：支持模糊分段与精确分段、场景变化检测（智能混合/HSV/SSIM/帧差/感知哈希）、目标时长窗口筛选、尾段回溯优化及多片段切片输出。
- 新增视频后端接口：`/xiheha_toolkit/video_info`（元数据探测）、`/xiheha_toolkit/split_status`（切分进度状态）与 `/xiheha_toolkit/split_video`（切分任务触发）。
- 新增轻量通用悬停卡片引擎 `web/shared/tooltip.js`，支持全局互斥、智能对齐与自适应定位。
- 前端集成智能视频分割器控制器 `web/nodes/smart-video-splitter.js` 与时长滑条交互控件。
- 完善 ComfyUI 插件开发技能规范与自动化校验工具链（支持环境校验、测试运行、前端 ESM 语法检查与导入验证）。

## 0.7.0 - 2026-09-19

- 「编辑提示词配置」节点在保存按钮左侧新增「删除」按钮，支持删除当前选中的配置项或清空新增草稿。
- 删除操作通过节点框内自定义遮罩卡片进行二次确认，确认后自动保存写回文件并重新连续编号。
- 优化工具栏布局：消除模型选择框与删除按钮之间的多余间隙，间距紧凑一致。
- 「编辑提示词配置」节点的模型下拉菜单支持点击其他区域或按 Escape 自动收起，并在节点重绘或销毁时清理全局监听器。
- 「easy-use兼容_模型列表获取」节点最小宽度限制为 150px，避免节点标题被过早截断。

## 0.6.1 - 2026-09-19

- 修复「编辑提示词配置」节点配置按钮未保存星号背景非透明（深色方块）的问题，改为透明背景。

## 0.6.0 - 2026-09-19

- 新增「编辑提示词配置」节点：支持模型下拉选择、配置新增与修改、未保存星号提示及一键保存全部草稿。
- 优化编辑节点的模型下拉菜单：列表背景匹配当前模型框的节点灰色，展开时模型框保持默认样式；修正长模型名与箭头重叠，让保存按钮与模型框等高，并收紧节点默认高度和底部留白。
- 新增本地受限保存接口；已有配置写回原 TXT/JSON sidecar，无配置文件时创建 `模型名.txt`。

## 0.5.1 - 2026-09-19

- `XH_SOURCE` 模型列表记录统一只保留 `source_name` 和 `folder_name`，移除不参与提示词配置的强度字段。

## 0.5.0 - 2026-09-19

- 新增「显示提示词」节点，分别显示正向和负向提示词，并透传两个提示词输出。

## 0.4.2 - 2026-09-19

- 修复「选择提示词配置」节点配置按钮文字上下不居中的问题。

## 0.4.1 - 2026-09-05

- 节点分类根目录由 `xiheha-工具库` 改为 `xiheha-工具箱`，来源节点子分类由 `模型加载器` 改为 `模型列表获取`。
- `easy-use兼容_来源获取` 改名为 `easy-use兼容_模型列表获取`。
- 端口名称统一：`堆` 改为 `Lora堆`，`来源列表` 改为 `模型列表`（含 `XH_StackSource` 输入/输出与 `XH_PromptSelector` 输入）。

## 0.4.0 - 2026-09-05

- 插件定位调整为通用工具库，提示词 sidecar 配置作为其中一个功能模块。
- 节点 ID 统一为 `XH_` 前缀，类名与内部标识同步通用化，不保留旧版 `LPT_` ID 兼容。
- 自定义数据类型 `LORA_PROMPT_SOURCE` 改为 `XH_SOURCE`；来源数据字段 `loras`/`lora_name` 统一为 `sources`/`source_name`，解析结果改为 `SourceInspection`（`display_name`）。
- `easy-use兼容_模型列表获取` 改名为 `easy-use兼容_来源获取`，输入改为必选 `stack`，输出端口改为「堆」「来源列表」。
- 本地预览接口路径改为 `/xiheha_toolkit/inspect`，执行回传 key 改为 `xh_rows`/`xh_ports`，前端 DOM class 与内部状态统一 `xh` 前缀。
- 前端由单文件 `lora_prompt_toolkit.js` 重构为原生 ES Module：入口 `web/xiheha_toolkit.js`，共享能力放 `web/shared/`，每个节点一个 `web/nodes/*.js` 模块（内聚 controller 与生命周期钩子）；样式参考文件改名 `web/toolkit.css`。行为不变，不保留旧前端文件。

## 0.3.0 - 2026-09-05

- 新增 `模型列表获取` 节点，支持透传 ComfyUI `MODEL` 并扫描基础模型 sidecar 配置。
- `选择提示词配置` 同时兼容基础模型和 LoRA 提示词配置源。

## 0.2.9 - 2026-09-05

- 模型列表获取节点的 `Lora堆` 输入改为必选输入。
- 节点分类库名称由 `LoraPromptToolkit` 改为 `xiheha-工具库`。

## 0.2.8 - 2026-09-05

- 插件英文目录和内部标识更新为 `xiheha-toolkit`。
- 更新插件标题及四个节点、端口和提示文字名称。
- 保留原有节点 ID，旧工作流连接无需重新建立。

## 0.2.7 - 2026-09-04

- 节点2输出的正向和负向提示词统一自动补齐末尾 ASCII 逗号。
- 空提示词保持为空，不添加多余逗号。

## 0.2.6 - 2026-09-04

- 预览节点切换提示词时只更新当前标签，不再重建整行内容。
- 修复长提示词横向滚动位置被重置到最左侧及界面闪烁问题。

## 0.2.5 - 2026-09-04

- 合并节点运行后始终保留四个输入内容显示框，空端口不再消失。
- 预览节点增加提示词开关，默认全部启用；关闭后标签变灰，运行时不会进入最终输出。
- 正向或负向内容为空时隐藏对应分区，模型两侧都为空时隐藏整个模型区块。

## 0.2.4 - 2026-09-04

- 统一节点2配置按钮与“无配置”提示的固定高度。
- 增强节点3不同模型之间的分割线显示。

## 0.2.3 - 2026-09-04

- 三个界面节点默认宽度调整为 400，高度保持 300。
- 滚动区域改为跟随节点实际内容高度自适应，节点拉高时同步扩大。
- 合并节点四个提示词输入全部强制注册为输入端口，其中第一个必选、后三个可选。

## 0.2.2 - 2026-09-04

- 节点2、节点3、节点4统一调整为默认最小尺寸 350×300。
- 缩小内部滚动内容区，保持内容在节点框内显示。

## 0.2.1 - 2026-09-04

- 为节点2、节点3、节点4增加固定内容区和滚动容器，防止内容超出节点边框。
- 增大三个界面节点的默认尺寸，并限制手动缩小时的最小尺寸。
- 节点3只有在模型名称、正向提示词、负向提示词三个端口全部连接时才实时预览。

## 0.2.0 - 2026-09-04

- 节点1改为纯 LoRA 堆适配器，不再维护本地 LoRA 选择和强度控件。
- 节点1实时读取上游 easy-use LoRA 堆的当前选择，并继续输出提示词源。
- 节点名称更新为「easy-use兼容Lora堆适配器」。

## 0.1.1 - 2026-09-04

- 节点1复刻 easy-use 的动态 LoRA 控件显示逻辑，仅展开当前 LoRA 数量。
- 修复连续选择多个 LoRA 时节点2异步刷新可能被旧结果覆盖的问题。
- 节点输出端口增加中文显示名称，并为输入端口补充中文显示标签。

## 0.1.0 - 2026-09-04

- 初始版本。
- 增加 easy-use 兼容 LoRA 堆及提示词源输出。
- 增加 TXT/JSON 正负向配置选择器。
- 增加正负向提示词预览和四端口合并节点。
- 增加 ANI 样例配置解析测试。
